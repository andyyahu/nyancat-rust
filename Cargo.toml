[package]
name = "nyancat"
version = "1.6.0"
edition = "2024"
rust-version = "1.85"
license = "NCSA"

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
